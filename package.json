{
  "name": "email-signature",
  "version": "1.0.0",
  "description": "",
  "main": "src/input.mjml",
  "scripts": {
    "watch": "npx mjml -w src/input.mjml -o dist/output.html",
    "build:prod": "npx mjml src/input.mjml -o dist/output.html --config.minify",
    "deploy": "node -e 'require(\"gh-pages\").publish(\"dist\", err => {} );'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "mjml": "^4.7.1"
  }
}
